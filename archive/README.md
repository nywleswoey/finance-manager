# archive

Retired process trees, kept for history, not live workflow. Do not treat anything here as
current documentation or as the agent entrypoint — see [AGENTS.md](../AGENTS.md).

- `aidlc-docs/` — the AIDLC construction log for the original build.
- `.aidlc-rule-details/` — the generic AIDLC workflow engine `CLAUDE.md` used to inject.
- `.wayfinder/` — local copies of closed GitHub Issues tracker tickets (map + tickets +
  research); GitHub Issues was and remains the canonical record.
- `one-shots/` — finished scripts kept so they can be re-read. Not on the live path;
  `pytest` does not collect them.
