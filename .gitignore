# python
.venv/
build/.venv/
__pycache__/
*.pyc
.env

# node / web build
web/node_modules/
web/dist/

# generated outputs
build/*.csv
portfolio.html

# raw personal statements (account numbers / addresses) — keep local, not in git
# (data/* not data/ so the one tracked file below can be re-included)
data/*
# …except the dividend-rate reference (tickers/dates/rates, no account numbers)
!data/dividends-master.csv
# …and the IBKR options record (tickers/strikes/premiums — IBKR flex carries no trades,
# so this hand-kept file is the only source; no account numbers)
!data/ibkr-options/
!data/ibkr-options/options.csv

# misc
.DS_Store
.playwright-mcp/
.vercel
.env*

.scratch
